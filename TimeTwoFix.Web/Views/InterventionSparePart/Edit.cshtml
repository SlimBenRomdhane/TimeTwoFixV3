@model TimeTwoFix.Web.Models.InterventionSparePartModel.UpdateInterventionSparePartViewModel

@{
    ViewData["Title"] = "Edit Intervention Spare Part";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container my-5">
    <div class="card border-0 shadow-sm">
        <div class="card-body">
            <h2 class="text-primary fw-bold mb-4">
                <i class="bi bi-pencil-square me-2"></i> Edit Intervention Spare Part
            </h2>
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                <input asp-for="Id" type="hidden" />
                <div class="row g-3">
                    <div class="col-md-6">
                        <label asp-for="InterventionId" class="form-label">Intervention</label>
                        <input asp-for="InterventionId" class="form-control" />
                        <span asp-validation-for="InterventionId" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="SparePartId" class="form-label">Spare Part</label>
                        <input asp-for="SparePartId" class="form-control" />
                        <span asp-validation-for="SparePartId" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Quantity" class="form-label">Quantity</label>
                        <input asp-for="Quantity" class="form-control" />
                        <span asp-validation-for="Quantity" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="DeliveryNote" class="form-label">Delivery Note</label>
                        <input asp-for="DeliveryNote" class="form-control" />
                        <span asp-validation-for="DeliveryNote" class="text-danger small"></span>
                    </div>
                </div>
                <div class="d-grid mt-4">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="bi bi-save me-1"></i> Save Changes
                    </button>
                </div>
            </form>
            <div class="d-grid mt-4">
                <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                    <i class="bi bi-arrow-left-circle me-2"></i> Back to List
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
